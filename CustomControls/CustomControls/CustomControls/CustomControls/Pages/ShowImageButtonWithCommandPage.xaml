<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:CustomControls.Controls"
             x:Class="CustomControls.ShowImageButtonWithCommandPage"
             Title="Image Button">
  <ContentPage.Content>
    <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
      <local:ImageButton VerticalOptions="Center" HorizontalOptions="Center"
                         ButtonText="Click me" Source="CameraIcon.png"
                         Command="{Binding MyCommand}"/>
      <Label Text="{Binding StatusMessage}" />
    </StackLayout>
  </ContentPage.Content>
</ContentPage>